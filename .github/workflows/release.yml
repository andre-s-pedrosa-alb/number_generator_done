name: Create Release

on:
  workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions-ecosystem/action-push-tag@v1
        with:
          tag: ya
          message: "cenas"
